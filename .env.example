# Environment variables for Home Assistant integration
# Copy this to .env and customize as needed

# Timezone for proper timestamps
TZ=America/New_York

# RTSP password for accessing Frigate streams
FRIGATE_RTSP_PASSWORD=your-secure-password-here

# Camera credentials (FRIGATE_ prefix required for use in config)
FRIGATE_CAMERA_USERNAME=admin
FRIGATE_CAMERA_PASSWORD=your-camera-password
FRIGATE_CAMERA_IP=192.168.1.100

# Home Assistant configuration
HA_INTERNAL_URL=http://homeassistant:8123
HA_EXTERNAL_URL=http://localhost:8123

# Cloudflare Tunnel configuration
# Get your tunnel token from https://one.dash.cloudflare.com/
CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here

# MQTT configuration (if using external MQTT broker)
# MQTT_HOST=homeassistant.local
# MQTT_PORT=1883
# MQTT_USER=frigate
# MQTT_PASSWORD=your-mqtt-password

# Optional: Hardware acceleration
# Uncomment the relevant lines in docker-compose.yml if using GPU/TPU