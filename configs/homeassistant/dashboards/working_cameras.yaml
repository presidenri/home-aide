# Working Camera Dashboard with actual entity names
title: Live Cameras
views:
  - title: Camera Feed  
    path: live
    icon: mdi:video
    cards:
      # Try to show the camera entity (will show if it exists)
      - type: picture-entity
        entity: camera.home_garage_main
        name: "Outdoor Camera"
        show_state: false
        show_name: true
        
      # Detection status using common entity patterns
      - type: entities
        title: "Detection Status"
        entities:
          - entity: binary_sensor.home_garage_main_person_occupancy
            name: "Person Detected"
          - entity: binary_sensor.home_garage_main_all_occupancy
            name: "Motion Detected"
        show_header_toggle: false
        
      # Camera controls
      - type: entities
        title: "Camera Controls"
        entities:
          - entity: switch.home_garage_main_detect
            name: "Detection Enabled"
          - entity: switch.home_garage_main_recordings
            name: "Recording Enabled"
        show_header_toggle: false
        
  - title: Activity
    path: activity
    icon: mdi:motion-sensor
    cards:
      # Status overview
      - type: glance
        title: "Current Status"
        entities:
          - entity: binary_sensor.home_garage_main_person_occupancy
            name: "Person"
          - entity: camera.home_garage_main
            name: "Camera"
            
      # System info
      - type: entities
        title: "Camera Performance"  
        entities:
          - entity: sensor.home_garage_main_camera_fps
            name: "Camera FPS"
          - entity: sensor.home_garage_main_detection_fps  
            name: "Detection FPS"
        show_header_toggle: false
        
  - title: Management
    path: management
    icon: mdi:cog
    cards:
      - type: markdown
        content: |
          ## Camera Management
          
          ### 🎥 Live Access
          - **Current Status**: Frigate integration working ✅
          - **Camera Feed**: Available in Home Assistant
          - **Detection**: Person and motion detection active
          
          ### 🔧 Useful Links
          - **[Frigate UI](http://localhost:8971)** - Full camera management
          - **[Media Browser](#)** - Browse recordings
          
          ### 📊 Entity Status
          Check **Developer Tools** → **States** for:
          - `camera.home_garage_main`
          - `binary_sensor.home_garage_main_*`  
          - `sensor.home_garage_main_*`
          - `switch.home_garage_main_*`
          
          If entities show as "unavailable", check:
          1. Frigate is running: `docker compose logs frigate`
          2. MQTT connection is working
          3. Camera network connectivity